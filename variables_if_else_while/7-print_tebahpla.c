#include <stdio.h>
/**
 *main - Prints if number is postive or negative.
 *
 *Return: Always 0.
 */
int main(void)
{
int i;
for (i = 25; i >= 0; i--)
{
putchar('a' + i);
};

putchar('\n');
return (0);
}
