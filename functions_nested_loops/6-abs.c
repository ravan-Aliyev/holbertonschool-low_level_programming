#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 *_isalpha - Check c is letter or not.
 *@n: is number for the check.
 *
 *Return: Absolute value of n.
 */
int _abs(int n)
{
	int y = abs(n);

	return (y);
}
