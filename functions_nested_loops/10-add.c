#include <stdio.h>
#include "main.h"
/**
 *add - Check c is letter or not.
 *@a: is number for sum.
 *@b: is number for sum.
 *
 *Return: Sum of a and b.
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
