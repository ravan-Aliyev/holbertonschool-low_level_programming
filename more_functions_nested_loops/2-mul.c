#include <stdio.h>
/**
 * _isupper - check character is upper or not.
 *@a: is number.
 *@b: is number
 *
 * Return: Multipiles of a and b
 */
int mul(int a, int b)
{
	int multi = a * b;

	return (multi);
}
