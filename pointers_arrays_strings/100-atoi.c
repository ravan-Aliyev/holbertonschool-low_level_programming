#include <stdio.h>
#include <string.h>
#include <stdlib.h>
/**
 * _atoi - Turn string into the number.
 * @s: string
 *
 * Return: Number.
*/

int _atoi(char *s)
{
	int num = atoi(s);

	return (num);
}
